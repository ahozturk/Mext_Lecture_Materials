using App.Domain;
using App.Domain.Entities;
using App.Domain.ValueObjects;

for (int i = 0; i < length; i++)
{
    if ()
    {
        var phone = PhoneFactory.CreatePhone("Samsung", "Galaxy S21");
    }
}